# Integration workflow to format, lint, build code on all supported platforms, and run tests.
# source for VCPKG goodness: https://github.com/lukka/CppCMakeVcpkgTemplate/blob/v11/.github/workflows/hosted-pure-workflow.yml
name: Integration

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Runs daily at 09:14 UTC = 03:14 CST
    - cron: "14 9 * * *"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  checks: write

jobs:
  build-and-test:
    runs-on: ${{ matrix.os }}
    concurrency:
      # Unique per workflow + PR/ref + matrix combination so only the matching
      # in-progress job is cancelled, not all matrix jobs simultaneously.
      group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}-${{ matrix.os }}-${{ matrix.cmake_preset }}-${{ matrix.cpp_compiler }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}

    strategy:
      # Set fail-fast to true to terminate the jobs as soon as any one fails. set to false for debug
      fail-fast: false

      # Set up a matrix to run the following configurations:
      # - <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # - <Linux,   Debug,   latest GCC compiler toolchain on the default runner image, default generator>
      # - <Linux,   Debug,   latest Clang compiler toolchain on the default runner image, default generator>
      # - <MacOs,   Release, latest Clang compiler toolchain on the default runner image, default generator>
      matrix:
        os: [self-hosted-ubuntu-24.04-arm64, windows-latest, macos-15]
        cmake_preset: [release, debug]
        cpp_compiler: [g++, clang++, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: self-hosted-ubuntu-24.04-arm64
            c_compiler: gcc
            cpp_compiler: g++
          - os: self-hosted-ubuntu-24.04-arm64
            c_compiler: clang
            cpp_compiler: clang++
          - os: macos-15
            c_compiler: clang
            cpp_compiler: clang++
        exclude:
          - os: self-hosted-ubuntu-24.04-arm64
            cmake_preset: release
          - os: windows-latest
            cmake_preset: debug
          - os: windows-latest
            cpp_compiler: g++
          - os: windows-latest
            cpp_compiler: clang++
          - os: self-hosted-ubuntu-24.04-arm64
            cpp_compiler: cl
          - os: macos-15
            cpp_compiler: cl
          - os: macos-15
            cpp_compiler: g++
          - os: macos-15
            cmake_preset: debug

    steps:
      # Checkout MUST happen before calling local composite action
      - name: ⤵️ Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
          submodules: true

      # ===== BUILD STEPS (calls composite action) =====
      - name: 🏗️ Build ASTL
        uses: ./.github/actions/build
        with:
          os: ${{ matrix.os }}
          cmake_preset: ${{ matrix.cmake_preset }}
          c_compiler: ${{ matrix.c_compiler }}
          cpp_compiler: ${{ matrix.cpp_compiler }}

      # ===== TEST STEPS =====
      - name: 🧪 CMake - Run Unit Tests and Generate Code Coverage
        if: contains(matrix.os, 'ubuntu')
        # Execute tests defined by the CMake configuration.
        # Note that --preset is needed because the default Windows generator
        # is a multi-config generator (Visual Studio generator).
        # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
        # exclude sample tests - we want to measure coverage of tests that have assertions in them
        run: |
          MEMCHECK_PARAMS=()
          if [[ "${GITHUB_EVENT_NAME}" == "schedule" && "${CMAKE_PRESET}" == "debug" ]]; then
            echo 'Running unit tests with Valgrind, since this is a scheduled debug run.'
            valgrind --version
            MEMCHECK_PARAMS=(--test-action memcheck)
          else
            echo 'Running unit tests without Valgrind.'
          fi

          ctest --parallel ${{ steps.cpu-cores.outputs.count }} -LE "integration|time_sensitive" "${MEMCHECK_PARAMS[@]}" --test-dir ${{ github.workspace }}/build/${{ matrix.cmake_preset }} --preset ${{ matrix.cmake_preset }}
          echo 'Running time_sensitive tests without parallelization or memcheck.'
          ctest -L "time_sensitive" --test-dir ${{ github.workspace }}/build/${{ matrix.cmake_preset }} --preset ${{ matrix.cmake_preset }}

          if [[ -n "$MEMCHECK_PARAM" ]]; then
            echo 'Checking valgrind output for memory issues'
            # Grep will return a non-zero exit code if the defect list is non-empty and cause the CICD flow to fail
            grep -F '<DefectList/>' ${{ github.workspace }}/build/${{ matrix.cmake_preset }}/Testing/*/DynamicAnalysis.xml
          fi
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          CMAKE_PRESET: ${{  matrix.cmake_preset }}

      - name: 📊 Code Coverage - Merge Coverage from Parallel Runs
        if: contains(matrix.os, 'ubuntu') && matrix.c_compiler == 'gcc' && matrix.cmake_preset == 'debug'
        run: ${{ github.workspace }}/scripts/merge_coverage.sh ${{ github.workspace }}/build

      - name: 📊 Code Coverage - Generate Report with gcovr
        if: contains(matrix.os, 'ubuntu') && matrix.c_compiler == 'gcc' && matrix.cmake_preset == 'debug'
        run: ${{ github.workspace }}/scripts/create_coverage_report.sh --xml

      - name: ⤴️ Code Coverage - Publish to Qlty
        # project overview: https://qlty.sh/gh/Arm-Debug/projects/ASTL
        if: contains(matrix.os, 'ubuntu') && matrix.c_compiler == 'gcc' && matrix.cmake_preset == 'debug'
        uses: qltysh/qlty-action/coverage@v2
        with:
          token: ${{ secrets.QLTY_COVERAGE_TOKEN }}
          files: coverage.xml

      - name: 🧪 Run Sample tests
        # Execute tests defined by the CMake configuration.
        # Note that --preset is needed because the default Windows generator
        # is a multi-config generator (Visual Studio generator).
        # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
        # include only sample tests, we skipped these earlier in `Unit tests`
        # because we don't want to include them for coverage.
        run: ctest -L "integration" --preset ${{ matrix.cmake_preset }}

      - name: 📽️ Run Demo with MockSysFs
        if: contains(matrix.os, 'ubuntu') && matrix.c_compiler == 'gcc' && matrix.cmake_preset == 'debug'
        run: |
          set -o pipefail
          EXPECTED_STRING="astlGetMetricSamplesOnTarget Status: SUCCESS"
          ${{ github.workspace }}/scripts/demo.sh > demo.log
          cat demo.log
          grep "${EXPECTED_STRING}" demo.log
        env:
          ASTL_LOG_LEVEL: "DEBUG"
          ASTL_LOG_CONSOLE: 1
