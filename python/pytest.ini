[pytest]
# Restrict discovery to this package's tests (relative to this config file location)
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
	slow: build or packaging integrity tests (sdist / wheel checks)
